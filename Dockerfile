# Використовуємо базовий образ Python версії 3.9
FROM python:3.12

# Встановлюємо робочу директорію /app в образі
WORKDIR /app

# Копіюємо файли додатку в робочу директорію образу
COPY . /requirements.txt .

# Встановлюємо залежності за допомогою pip
RUN pip install --no-cache-dir -r requirements.txt

# Копируем все файлы проекта в контейнер
COPY . .

# Вказуємо команду для запуску застосунку при старті контейнера
CMD ["python", "__main__.py"]



